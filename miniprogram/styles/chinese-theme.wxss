/* ========================================
   命理大师 - 宣纸水墨主题
   Rice Paper & Ink Theme
   ======================================== */

.chinese-border {
  position: relative;
  border: 1rpx solid #E5E0D8;
  border-radius: 16rpx;
  overflow: hidden;
  background: #FFFFFF;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.04);
}

.chinese-border::before,
.chinese-border::after {
  content: '';
  position: absolute;
  width: 24rpx;
  height: 24rpx;
  border: 2rpx solid #3D6B5E;
}

.chinese-border::before {
  top: 8rpx;
  left: 8rpx;
  border-right: none;
  border-bottom: none;
}

.chinese-border::after {
  bottom: 8rpx;
  right: 8rpx;
  border-left: none;
  border-top: none;
}

.scroll-container {
  background: #FFFFFF;
  border: 1rpx solid #E5E0D8;
  border-radius: 16rpx;
  padding: 32rpx;
  position: relative;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.04);
}

.zen-divider {
  height: 1rpx;
  background: linear-gradient(90deg, transparent, #E5E0D8, #3D6B5E, #E5E0D8, transparent);
  margin: 24rpx 0;
  opacity: 0.6;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(20rpx); }
  to { opacity: 1; transform: translateY(0); }
}

.animate-fade-in {
  animation: fadeIn 0.5s ease-out forwards;
}

.hexagram-symbol {
  font-family: serif;
  font-size: 80rpx;
  color: #3D6B5E;
}

.fortune-badge {
  display: inline-flex;
  align-items: center;
  padding: 4rpx 16rpx;
  border-radius: 20rpx;
  font-size: 22rpx;
  font-weight: bold;
}

.fortune-badge.excellent {
  background: rgba(74, 124, 138, 0.12);
  color: #3D6B5E;
  border: 1rpx solid rgba(74, 124, 138, 0.25);
}

.fortune-badge.good {
  background: rgba(91, 138, 114, 0.12);
  color: #3D6B5E;
  border: 1rpx solid rgba(91, 138, 114, 0.25);
}

.fortune-badge.neutral {
  background: rgba(122, 122, 122, 0.12);
  color: #888888;
  border: 1rpx solid rgba(122, 122, 122, 0.25);
}

.fortune-badge.poor {
  background: rgba(184, 149, 42, 0.12);
  color: #C4965A;
  border: 1rpx solid rgba(184, 149, 42, 0.25);
}
